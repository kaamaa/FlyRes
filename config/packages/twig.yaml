twig:
    default_path: '%kernel.project_dir%/templates'
    debug: '%kernel.debug%'
    strict_variables: '%kernel.debug%'
    globals:
        # Versionsnummer ueberall im UI verfuegbar (eine Quelle: services.yaml)
        app_version: '%app_version%'

